source_shellfiles() {
  readonly shellfiles=$(find $DOTFILES_DIR -type f \( -name "*.zsh" -o -name "*.dotdir" \) -mindepth 2 -maxdepth 2)

  for shellfile in $shellfiles; do
    source "$shellfile"
  done
}

source_shellfiles
